# Quartz build output
public/

# Quartz framework (gegenereerd door postinstall)
/quartz

# Dependencies
node_modules/

# IDE
.idea/
.vscode/

# OS
.DS_Store

# Resources — bulk raw downloads (PDFs, txt, xlsx, ...)
# Alleen de bulk-mappen worden geignoreerd; hand-curated raw inputs
# (voorbeeldexamens, andere/) blijven tracked omdat ze niet auto-fetchable zijn.
resources/raw/wetteksten/
resources/raw/normen/

**/__pycache__/
*.pyc

# PO-build voortgang (lokaal, niet in repo)
.po-voortgang-*.md

# RAG data (gegenereerd, herbouwbaar)
data/chroma_db/
data/chroma_db_*/

# Concept-records (gegenereerd door extractor — gitignored want herbouwbaar;
# geverifieerde records kunnen later tracked worden wanneer status = geverifieerd)
data/concept_records/
data/cases/
data/relations.json

# Extractie-output: vermoedens worden WEL getracked (curatie-artefact, niet auto-herbouwbaar)
# data/extractie/ is dus NIET geignoreerd

# Python virtual envs
venv/
.venv/

# Environment variables
.env

# Review-artifacts (gegenereerd door review-agents)
.factcheck-*.md
.leesbaarheid-*.md
.student-feedback*.md
