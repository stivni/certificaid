# Embedding-daemon (ADR-018)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# RAG-infrastructuur
chromadb>=0.5.0
sentence-transformers>=3.0.0
# bge-m3 embedding model + bge-reranker-v2-m3 cross-encoder worden automatisch
# gedownload via sentence-transformers bij eerste gebruik (~570MB elk)

# Claude API
anthropic>=0.40.0

# Tutor interface
streamlit>=1.35.0

# Markdown + YAML parsing
python-frontmatter>=1.1.0

# Utilities
tqdm>=4.66.0

# MCP-server voor extract-pipeline (ADR-025 §EXTRACT v5)
mcp>=1.27.0
